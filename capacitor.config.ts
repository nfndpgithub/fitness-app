import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mmklab.fitness',
  appName: 'FitLife',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
